---
title: "CoVStream: Edge-Cloud Collaboration for Understanding of Long Video Streams"
title_zh: CoVStream：面向长视频流理解的边云协同
authors: "Xu Liu, Guikun Chen, Zihao Yan, Kanzhi Wu, Wenguan Wang"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.22804v1"
tags: ["query:long-video"]
score: 9.0
evidence: 边缘云协作理解长视频流
tldr: "长视频流智能理解面临设备算力受限与云卸载带宽高的矛盾。现有方法要么在端侧处理受限，要么云传输开销大。CoVStream提出边缘-云协作框架：边缘节点将视频流蒸馏为紧凑视觉特征和语义描述，传输至云端；云端构建实体图和全局上下文，仅在用户查询时激活重型推理。在VideoMME-Long等基准上，带宽降低87.6%，LVBench准确率保持99.2%。首个兼顾效率与精度的边缘-云协作方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.22804v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 874, \"height\": 630}, {\"url\": \"assets/figures/arxiv/2606.22804v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1815, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2606.22804v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1829, \"height\": 552}, {\"url\": \"assets/figures/arxiv/2606.22804v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1634, \"height\": 710}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.22804v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1815, \"height\": 316}, {\"url\": \"assets/tables/arxiv/2606.22804v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 910, \"height\": 389}, {\"url\": \"assets/tables/arxiv/2606.22804v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 842, \"height\": 208}]"
motivation: 现有长视频流处理方法中，云卸载导致高带宽成本，而端侧处理受限于边缘硬件能力，需平衡推理性能与传输开销。
method: 边缘节点将视频流蒸馏为紧凑视觉特征与语义描述；云端整合为实体图和全局上下文，仅在有查询时启用大模型推理。
result: "在VideoMME-Long、LVBench等上，带宽减少87.6%，LVBench准确率保持99.2%，接近云基线。"
conclusion: CoVStream通过边缘-云协作，显著降低带宽开销同时维持高精度，适用于资源受限的长视频流理解场景。
---

## 摘要
长连续视频流是多媒体智能日益关键的驱动因素。现有工作通常采用“采样-编码-推理”的方式，利用大模型处理长视频。然而，它们忽略了一个关键的部署事实：视频流通常由计算受限的设备产生。这导致了一个难以持续的妥协：云端卸载能实现强推理但带来高昂的带宽开销，而设备端处理仍受限于边缘硬件能力。因此，我们提出了CoVStream，首个面向长视频流理解的边云协同框架。边缘节点将原始视频流提炼为紧凑的视觉特征和语义描述用于传输到云端，最小化带宽成本，而云服务器将这些数据整合为实体图和全局视觉上下文，仅在用户查询到达时激活重型推理模型。在VideoMME-Long、LVBench和RTV-Bench上的实验表明，CoVStream在LVBench上减少了87.6%的带宽使用，同时保留了云端基线准确率的99.2%。

## Abstract
Long, continuous video streams are an increasingly critical driver of multimedia intelligence. Existing efforts often handle long videos with a sample-encode-reason approach using large models. However, they overlook a crucial deployment fact: the stream is often produced by computationally constrained devices. This forces an untenable compromise: cloud offloading unlocks strong reasoning but incurs prohibitive bandwidth overhead, while on-device processing remains limited by edge hardware capacity. Therefore, we propose CoVStream, the first edge-cloud collaborative framework for understanding long video streams. The edge node distills raw video streams into compact visual features and semantic captions for transmission to the cloud, minimizing bandwidth costs, while the cloud server integrates this data into an entity graph and global visual context, activating the heavy reasoning model only when a user query arrives. Experiments on VideoMME-Long, LVBench, and RTV-Bench show that CoVStream reduces bandwidth usage by 87.6% while retaining 99.2% of the cloud baseline accuracy on LVBench.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：长连续视频流理解在计算受限的边缘设备（如智能眼镜）上部署面临两难困境：
  - **云卸载**（Cloud-Only）：传输原始视频流带宽成本高（≥2549.78 MB/会话），难以持续运行。
  - **端侧处理**（Local-Only）：受限于边缘硬件算力，无法部署重型多模态大模型（LMM），任务准确率低（如LVBench仅32.42%）。
- **目标**：设计一种边云协同框架，同时利用云端强推理能力和边缘低传输开销，实现**高精度、低带宽、低延迟**的长视频流理解。

## 2. 方法论
- **核心思想**：利用视频流中的时空冗余，边缘作为“语义压缩器”将原始像素蒸馏为紧凑表示（视觉特征+文本描述），云端负责长期记忆维护和按需推理，推理模型仅在用户查询时激活。
- **关键技术细节**：
  - **边缘端：双流感知压缩流水线**（Dual-Stream Perception and Condensation Pipeline）
    1. **视觉特征提取**：用VideoLLaMA3-7B的视觉编码器（0.4B）逐帧提取特征 `f_t = E_vis(v_t)`，送入特征队列。
    2. **时序特征凝聚**：对队列中连续特征，迭代合并相似度最高的相邻特征对（式2），直至压缩至预设数量 `N_target`（默认16→2，压缩率87.5%），得到紧凑视觉特征 `Z_cond`。
    3. **自适应关键帧选取**：使用轮廓系数最优的聚类算法从缓冲窗口 `N_w=64` 中选取关键帧集 `K`（式3），送入关键帧队列。
    4. **语义描述生成**：用轻量语言模型Qwen3-VL-2B为每个关键帧生成严格简单句式的描述 `C_text`（式4），避免解析歧义。
    - **硬件调度**：GPU负责视觉提取和描述生成（密集计算），CPU负责特征凝聚和关键帧选取（动态聚类），消除资源竞争。
  - **云端：解耦管理-推理架构**（Decoupled Management and Reasoning Architecture）
    - **记忆管理**（持续运行）：
      - **全局视觉上下文维护**：接收 `Z_cond`，用迭代合并算法维护有限容量 `N_bank=128` 的压缩库 `H_t`。
      - **实时实体图构建**：对描述 `C_text` 进行句法分析（SpaCy）提取SVO三元组，映射为节点（实体）和有向边（动作）；利用NLI分类器（NLI-Deberta-v3）融合同义实体（双向蕴含判定）；用Qwen3-Embedding-0.6B将描述编码并挂载到节点，形成动态图 `G_t`。
    - **按需推理**（事件驱动）：
      - **睡眠-唤醒机制**：默认睡眠，收到边缘触发后立即激活。
      - **图增强推理**：将用户查询 `Q` 向量化，在图节点中检索top-3相似描述，以锚点为中心提取一阶邻居子图 `G_sub`，序列化为JSON，与全局视觉上下文 `H_t` 一起输入LMM（VideoLLaMA3-7B）生成回答 `R`（式10）。

## 3. 实验设计
- **数据集与场景**：三个长视频流基准：
  - **VideoMME-Long**：平均时长3160秒，900道选择题，覆盖6大视觉领域30子域。
  - **LVBench**：平均时长5018秒，1007道题，重点评估超长视频和“大海捞针”检索。
  - **RTV-Bench**：平均1092秒，167.2小时流式视频，涵盖自我中心、智能驾驶、体育分析等动态任务。
- **对比方法**：
  - **Local-Only (LO)**：用轻量VideoLLaMA3-2B本地处理所有帧，作为准确率下界。
  - **Cloud-Only (CO)**：用VideoLLaMA3-7B在云端处理1FPS原始视频，作为准确率上界。
  - **Edge-Cloud Hybrid (EC)**：边缘1FPS采样+4×下采样，云端用AuraSR-v2超分辨率恢复，然后推理。

## 4. 资源与算力
- **模型**：边缘使用VideoLLaMA3-7B的视觉编码器（0.4B参数）和Qwen3-VL-2B（2B参数）；云端使用VideoLLaMA3-7B（7B参数）。
- **GPU配置**：
  - 默认测试：NVIDIA A40 (48GB)。
  - 边缘压力测试：RTX 3090 (24GB) 和 RTX 2080 (8GB)，准确率稳定（38.63%~39.32%）。
- **训练情况**：CoVStream是**无需训练**（training-free）的方法，所有组件均在LMM原始特征空间中运行，可即插即用。因此不涉及训练时长和算力。

## 5. 实验数量与充分性
- **实验组数**：
  - 主表（表1）：在三个数据集上与三种基线对比（共3×4=12组），包含准确率、带宽、压缩率、延迟。
  - 消融实验（表2）：7种组件组合（T/S/G），验证各模块贡献。
  - 压缩策略权衡（图3a）：改变窗口大小（4/8/16/32）和压缩率（68.75%~93.75%），共多种配置。
  - 时间鲁棒性（图3b）：按视频长度分段（1000s起至>6000s）对比各方法准确率变化。
  - 内存可扩展性（图3c）：模拟24小时（90000秒）流式输入，监测内存增长趋势。
  - 硬件压力测试（表3）：三种GPU下边缘模型准确率。
  - 附录中还包括开放域QA、传输格式对比、网络压力测试、银行容量消融等（正文提及但未展示）。
- **充分性与公平性**：实验设计较为全面，覆盖不同时长、不同任务类型、不同硬件平台。对比基线包括纯端、纯云和简单混合方法，差异解释合理。多次运行标准差（表1中±std）表明结果稳定。未发现明显偏向性。

## 6. 主要结论与发现
- **性能平衡**：CoVStream在LVBench上达到38.93%准确率（保留CO的99.2%），同时带宽降低87.6%，延迟仅2.99秒（优于CO的4.08秒）。
- **超长视频优势**：在>6000秒的视频中，CoVStream准确率（42.92%）**超越**Cloud-Only（35.00%）7.92%，得益于结构化图检索避免了LMM上下文窗口限制。
- **内存可持续**：24小时连续流模拟中，基线内存线性增长至229.5 GB，CoVStream仅需438.3 MB（523×减少），且图部分仅增长至122.3 MB。
- **组件有效性**：消融实验显示，实体图（G）贡献最大（ID6→7提升4.27个百分点），而缺少任一组件（T或S）均会导致性能下降。

## 7. 优点
- **无需训练**：在LMM原始特征空间操作，即插即用，兼容多种LMM。
- **带宽高效**：双流压缩（特征+描述）实现87.6%压缩率，传输成本接近零（相比于原始视频）。
- **低延迟响应**：云端记忆管理持续进行，推理仅在查询时触发，消除累积延迟。
- **硬件自适应**：边缘流水线通过硬件亲和调度，在8GB GPU上仍能保持精度。
- **强鲁棒性**：超长视频中表现超过云基线，结构化图检索克服了LMM上下文窗口瓶颈。
- **实验充分**：覆盖多种基准、消融、压力测试和可扩展性分析，验证充分。

## 8. 不足与局限
- **对硬件仍有依赖**：边缘端需要至少8GB GPU（RTX 2080可运行），但更轻量的终端（如手机）未测试（仅提供演示视频）。
- **图构建依赖NLI**：实体融合使用的NLI分类器（NLI-Deberta-v3）可能引入错误，影响图质量。
- **未涉及隐私问题**：边云协同需上传语义描述，虽已压缩但未讨论数据隐私和安全性。
- **对比基线有限**：仅与LO、CO、EC三种简单基线对比，未与其他端侧部署方案（如模型量化、知识蒸馏）或更复杂的混合推理方法比较。
- **仅评估特定模型**：实验基于VideoLLaMA3-7B和Qwen系列，泛化性未在多架构上验证。
- **附录内容未公开**：正文中提及的开放域QA、传输格式等详细实验未在本文中展示，完整性稍欠。

（完）
