---
title: "Rosetta Memory: Adaptive Memory for Cross-LLM Agents"
title_zh: "Rosetta Memory: 面向跨LLM智能体的自适应记忆"
authors: "Hao Yang, Shiqi Shen, Haoxuan Li, Zhipeng Wang, Zhi Gong, Xu Chen"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07711v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 跨LLM代理的自适应内存系统
tldr: 现有记忆系统以特定LLM为中心，跨LLM使用时记忆不匹配。本工作提出记忆为中心的LLM适应方法，通过读写两端profile条件算子联合训练，并引入最小增益采样课程和性能差距奖励，使上游记忆有效激活下游LLM。在多个问答数据集上超过基线，且对未见模型鲁棒。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07711v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1426, \"height\": 872, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1452, \"height\": 394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1437, \"height\": 348, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1349, \"height\": 847, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1433, \"height\": 1184, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1456, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.07711v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1455, \"height\": 719, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.07711v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1459, \"height\": 717, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.07711v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1457, \"height\": 376, \"label\": \"Table\"}]"
motivation: 用户频繁切换LLM，记忆由一个模型写入却需由另一模型读取，现有记忆系统缺乏跨LLM自适应能力。
method: 从LLM中心转向记忆中心，设计写端和读端两个profile条件算子联合训练，用最小增益采样课程优先训练未充分服务的LLM，并用性能差距奖励衡量算子贡献。
result: 在HotpotQA、2WikiMultihopQA、MuSiQue上一致超越基线，且在未见模型替换下保持鲁棒。
conclusion: 提出记忆中心的自适应框架，有效实现跨LLM记忆迁移，提升agent持续学习能力。
---

## 摘要
记忆是将无状态的LLM转化为持久、进化的智能体的关键组件，通过经验积累、长时规划与持续自我改进。现有记忆系统通常以LLM为中心，并针对特定骨干设计记忆操作。然而在实践中，用户经常在不同LLM之间切换，例如在编码任务中使用Claude，在写作任务中使用GPT，或在一个任务中将不同步骤路由至不同骨干以实现成本效益权衡。因此，一个模型写入的记忆往往需要被另一个模型消费。如何使上游记忆有效适应并激活下游LLM仍然是一个关键但未充分探索的问题。为填补这一空白，我们将视角从以LLM为中心的记忆设计转向以记忆为中心的LLM自适应。具体而言，我们从写入端和读取端两个方向处理上述上游-下游记忆适应问题，并设计两个基于配置条件的算子，通过联合训练来优化记忆的存储与呈现方式，以提升任务完成效果。为确保学习的算子能在大量LLM上泛化，我们提出一种最小增益采样课程，在训练中优先处理服务最少的LLM。为了更好地衡量算子的实际贡献而非LLM自身能力，我们设计了一种性能差距奖励，以与朴素记忆基线进行比较。在HotpotQA、2WikiMultihopQA和MuSiQue上的实验表明，我们的模型始终优于基线，并且在未见模型替换下保持稳健。

## Abstract
Memory is the key component for transforming a stateless LLM into a persistent, evolving agent through experience accumulation, long-horizon planning, and continual self-improvement. Existing memory systems typically take the LLM as the center and design memory operations tailored to a specific backbone. In practice, however, users frequently switch between LLMs, for example using Claude for coding and GPT for writing across tasks, or routing different steps to different backbones within a single task for cost-effective trade-offs. As a result, memory written by one model often needs to be consumed by another. Making upstream memory effectively adapt to and activate downstream LLMs remains a critical yet underexplored problem. To bridge this gap, we shift the perspective from LLM-centric memory design to \emph{memory-centric LLM adaptation}. Specifically, we approach the above upstream-downstream memory adaptation problem from both the write and read sides, and design two profile-conditioned operators that are jointly trained to optimize how memory is stored and presented for better task completion. To ensure the learned operators generalize across a broad set of LLMs, we propose a minimum-gain sampling curriculum that prioritizes the least-served LLMs during training. To better measure the operators' actual contribution rather than the LLM's own capability, we design a performance-gap reward that compares against a naive memory baseline. Experiments on HotpotQA, 2WikiMultihopQA, and MuSiQue demonstrate that our model consistently outperforms baselines and remains robust under unseen-model replacement.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有记忆系统以固定LLM为中心设计记忆操作，但实际应用中用户频繁切换LLM（如编码用Claude、写作用GPT），导致一个模型写入的记忆需由另一个模型读取，产生“记忆不匹配”问题——写入端不同模型产生不同记忆内容，读取端相同记忆语义在不同模型上引发不同回答，降低任务性能。
- **研究背景**：记忆是将无状态LLM转化为持久、进化智能体的关键组件，但现有方法（如记忆流、技能库、层次记忆管理）均假设骨干模型在记忆生命周期中不变。随着多模型路由、级联等异构LLM系统兴起，跨模型记忆迁移成为亟需解决的问题。
- **整体含义**：将视角从“LLM中心记忆设计”转向“记忆中心LLM适应”，形式化了跨LLM记忆适应问题，旨在让上游写入的记忆能有效激活下游消费模型，提升异构LLM agent系统的鲁棒性和可持续性。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：设计两个LLM感知的读写算子（profile-conditioned write/read operators），通过联合训练学习如何将记忆存储为跨模型可迁移的证据，并以目标模型适配的形式呈现。同时引入最小增益采样课程和性能差距奖励，提升对最弱势LLM的覆盖并分离算子贡献。
- **关键技术细节**：
  - **Profile嵌入**：每个LLM关联轻量级profile（模型ID、文本描述、元数据），通过可学习映射函数 \(g\) 转换为连续条件向量 \(e_{L(k)}\)，注入读写算子作为软前缀（prefix tuning）。
  - **写入算子 \(f_{write}\)**：接收原始观察-动作对和源LLM的profile嵌入，生成标准化、去噪、避免直接答案泄露的记忆条目。
  - **读取算子 \(f_{read}\)**：接收当前观察、记忆库和目标LLM的profile嵌入，生成适配目标LLM呈现风格的上下文（如调整顺序、显式性、答案格式）。
  - **训练过程**：Expert Iteration式自训练循环——当前算子生成混合LLM轨迹，保留最高奖励的前\(\alpha\)比例作为专家演示，监督更新读写算子。具体步骤见Algorithm 1。
  - **最小增益采样**：定义每个LLM的counterfactual gain \(\Delta R(L^{(k)})\)（与朴素记忆基线相比的奖励提升），采样概率 \(p \propto d_t^{(k)} \exp(-\eta \Delta R)\)，其中\(d_t^{(k)}\)为与已采样模型在嵌入空间的最小距离，鼓励多样性和覆盖弱势模型。
  - **性能差距奖励**：\(\Delta R = R_f - R_{\text{naive}}\)，衡量算子真实贡献，避免LLM自身能力混淆。
  - **训练目标**：最大化高奖励轨迹中读写步的似然，联合优化\(\theta_w, \theta_r, \theta_g\)。

## 3. 实验设计：数据集、场景、对比方法

- **数据集**：三个多跳QA数据集，自然包含中间证据累积，适合暴露跨LLM记忆效应。
  - HotpotQA（2跳）
  - 2WikiMultihopQA（3跳）
  - MuSiQue（4跳）
  - 均过滤少于所需跳数的样本，训练/测试集大小分别为：HotpotQA 1851/159；2Wiki 642/67；MuSiQue 481/62。
- **场景**：多跳推理过程中，每步使用不同LLM（从6个API模型池中采样），记忆需跨模型传递。评估时保持子集固定，多次重复。
- **对比方法**：
  - **记忆无关**：Direct、CoT、Scratchpad、Self-Ask、Least-to-Most。
  - **原始缓冲**：Step-Accumulate（原始拼接）、Running-Summary（压缩摘要）。
  - **结构记忆**：Mem0、A-Mem、Memento（均无骨干感知自适应）。
- **评估指标**：Exact Match (EM)、Token-level F1、Acc（归一化包含分数）、Acc_LLM（GPT-5.4-mini语义判断）。
- **额外实验**：
  - 消融研究（去掉\(f_{write}\)、\(f_{read}\)、profiles）。
  - 模型替换泛化：用6个未见模型替换训练池模型，评估F1。
  - 最小增益采样系数\(\eta\)灵敏度（0.4~2.0）。
  - 定性分析读写输出示例。
  - 附加诊断实验：writer-reader兼容性热力图、最差目标读者增益、噪声记忆鲁棒性。

## 4. 资源与算力

- 文中仅提及：训练在 **NVIDIA A800-SXM4-80GB GPUs** 上进行，未明确数量、训练时长或总GPU小时数。
- 训练细节：使用AdamW优化器、batch size 4、梯度累积4、学习率搜索{1e-5, 2e-5, 3e-5}，权重衰减0.01，warmup ratio 0.03，最大源长度512，目标长度160。两个外层Expert Iteration轮次，每轮每训练示例采样10条混合LLM轨迹。
- 总体算力信息不够具体，无法精确估算。

## 5. 实验数量与充分性

- **实验组数**：
  - 主结果表1：三个数据集 × 四种指标 × 约10种方法 → 大量对比。
  - 消融表2：四个变体 × 三个数据集 × 四种指标。
  - 泛化实验图2：两个数据集 × 7个替换级别 × 3种方法。
  - 灵敏度实验图3：一个参数η × 三个数据集 × 三个指标。
  - 定性分析：一个示例。
  - 附加诊断（附录）：writer-reader热力图（7×7矩阵，两个数据集）、最差增益分析（6个目标读者，3个随机分配）、噪声鲁棒性（3个噪声级别 × 3种目标读者）。
- **充分性与公平性**：
  - 对比方法覆盖prompting、累积、结构记忆三大类，且均在相同子集、上下文来源、答案模型下评估。
  - 消融实验分离各个组件贡献。
  - 泛化实验验证对未见模型的鲁棒性。
  - 灵敏度实验分析超参数影响。
  - 额外诊断实验从兼容性、最坏情况、噪声角度补充验证。
  - 但是：评估仅限多跳QA，未覆盖开放域导航、工具使用、对话等；数据集经过过滤，规模较小；LLM池有限（仅6个训练模型、6个未见模型）。总体而言，实验设计系统全面，但在任务多样性和规模上存在局限。

## 6. 论文的主要结论与发现

- **主要结论**：Rosetta Memory在所有三个数据集上均显著超越所有基线，验证了跨LLM记忆自适应有效性。特别是语义准确度（Acc_LLM）提升最大，表明读写自适应改善了超越表面形式匹配的语义一致性。
- **消融发现**：去除profile嵌入或写入算子会大幅下降，读取算子同样重要，但移除写入算子的损失更大，表明中性化证据存储是关键。
- **泛化发现**：即使替换多达6个未见LLM，RoMem仍保持最优或接近最优，说明profile条件化接口能泛化到新模型。
- **采样发现**：适当强度（η≈1.2-1.6）的最小增益采样带来最佳性能，过强则因过度聚焦少数低增益模型而下降。
- **定性发现**：写入算子能去除源模型的不确定性、避免答案泄露；读取算子能根据目标模型调整呈现方式（如列表、推理链、直接答案）。
- **附加发现**：直觉上，RoMem使writer-reader兼容性更均匀（对角线优势减弱），并改善最差目标读者情况和噪声记忆鲁棒性。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：首次明确形式化跨LLM记忆适应问题，从“LLM中心”转向“记忆中心”，设计profile条件的读写算子，简洁有效。
- **训练设计**：最小增益采样与性能差距奖励协同工作，精准提升最弱势LLM的适应能力，避免采样偏差。
- **实验深度**：不仅展示平均表现，还深入分析writer-reader兼容性（热力图）、最坏情况增益、噪声容忍度，揭示方法的内在行为。
- **泛化验证**：通过替换未见模型证明接口的可迁移性，增强实用价值。
- **定性分析**：通过具体示例展示读写算子实际上如何转换文本，增强可解释性。
- **公平性**：所有方法共享相同候选上下文、答案模型、指标，消融全面，超参数调优公开。

## 8. 不足与局限

- **任务覆盖窄**：仅评估多跳QA（固定长度证据链），未覆盖开放网页导航、工具使用、长时对话、大规模规划等更复杂场景。
- **规模限制**：数据集经过过滤（仅几百个训练样本），LLM池有限（6个训练模型+6个未见模型），可能不足以反映真实异构系统的多样性。
- **资源消耗不透明**：GPU数量、训练时长未报告，难以评估可复现性和计算成本。
- **算子固有问题**：写入算子可能继承源模型不完整记忆或保留不确定性；读取算子可能过度压缩、删除关键桥接事实。
- **安全与伦理风险**：若记忆包含错误、偏见、隐私或受版权内容，学习者可能将其保留并以更权威形式传播，跨模型迁移会放大风险。论文虽在Broader Impact中提及，但未提出明确防护措施。
- **实验设计固有局限**：使用LLM-as-judge（Acc_LLM）存在偏差；基线中某些方法（如Memento）需要额外提示设计，可能未完全优化。

（完）
