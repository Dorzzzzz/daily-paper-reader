---
title: Verified Detection and Prevention of Concurrency Anomalies in Multi-Agent Large Language Model Systems
title_zh: 多智能体大语言模型系统中并发异常的验证检测与预防
authors: Sajjad Khan
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17182v1"
tags: ["query:agent-memory"]
score: 8.0
evidence: 多智能体记忆共享与并发异常
tldr: 多智能体LLM系统通过内存存储、向量索引和工具注册表共享状态，这会导致类似数据库隔离异常的并发问题。本文形式化定义了四种并发异常，并建立了从L0到L4的一致性层次。通过Verus验证了检测器的正确性，并在Rust运行时时实现了不同层次。在ByteDance的deer-flow和LangGraph中复现并修复了异常，证明了该方法的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17182v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 798, \"height\": 530, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17182v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 883, \"height\": 584, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17182v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1518, \"height\": 283, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17182v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 947, \"height\": 226, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17182v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 430, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17182v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 887, \"height\": 217, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17182v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1177, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17182v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 932, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17182v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 671, \"height\": 1029, \"label\": \"Table\"}]"
motivation: 多智能体LLM系统中的并发异常尚未被系统研究，现有方案缺乏形式化验证和严格的一致性层次。
method: 用TLA+形式化四种并发异常，定义一致性层次，并通过Verus对检测器和运行时进行机械验证。
result: 验证了检测器对规范的完备性和可靠性，部署了三个Rust运行时，并在实际系统中复现并修复了异常。
conclusion: 首次为多智能体LLM运行时提供了机器检查的一致性层次，并证明了可验证的检测与预防方法。
---

## 摘要
多智能体LLM系统通过内存存储、向量索引和工具注册表共享状态。我们将这种共享建模为在确定性生成语义（持久执行引擎通过确定性重放强制执行的机制）下的长时间运行的读-生成-写操作，并在TLA+中形式化了四种并发异常：过时生成、幻影工具、因果级联和工具效应重排序——它们是经典隔离异常的结构类似物，每种都附带TLC反例。这些异常上的排斥格是平凡的；贡献在于对该格中一个最大链$L_0 \subsetneq \cdots \subsetneq L_4$的机械验证的可实现性和严格分离——据我们所知，这是此类运行时首个机器检查的一致性层级。由274个Verus义务（零assume，零admit；信任基础：两个结构公理和一个互斥对应关系）开发证明检测器对规范是可靠且完备的，并且每个运行时其避免集均被验证。三个已部署的Rust运行时实现了L0-L1（悲观锁、可序列化快照隔离、默认-SI），每个都针对过时生成进行了验证并精化到其状态机；L2-L4通过执行模式验证，配有无依赖的预防孪生体（A3、A6、A2：0/1000对1000/1000），并且L2在三个模型系列上实时运行（在所有120个撤回会话中A3被预防）。我们再现了字节跳动deer-flow中一个静默的丢失更新，将其修复形式化为一个经过验证的$L_0 \to L_1$精化，并在LangGraph的ToolNode上展示了未经修改输出的工具效应重排序，通过一个L3提交顺序定序器消除。贡献在于经过验证的检测器、精化和可实现性工件；现象和格是经典的。

## Abstract
Multi-agent LLM systems share state through memory stores, vector indices, and tool registries. We model such sharing as long-running read-generate-write operations under deterministic-generation semantics -- the regime durable-execution engines enforce by deterministic replay -- and formalize four concurrency anomalies in TLA+: stale-generation, phantom-tool, causal-cascade, and tool-effect reordering, structural analogues of classical isolation anomalies, each with a TLC counter-example. The exclusion lattice over these anomalies is trivial; the contribution is the mechanically verified realizability and strict separation of one maximal chain within it, $L_0 \subsetneq \cdots \subsetneq L_4$, to our knowledge the first machine-checked consistency hierarchy for such runtimes. A development of 274 Verus obligations (zero assume, zero admit; trust base: two structural axioms and a mutex correspondence) proves the detectors sound and complete against the specifications and each runtime its avoidance set. Three deployed Rust runtimes realize L0-L1 (pessimistic locking, serializable snapshot isolation, default-SI), each verified against stale-generation and refined to its state machine; L2-L4 are exec-mode-verified with dependency-free prevention twins (A3, A6, A2: 0/1000 versus 1000/1000), and L2 is run live across three model families (A3 prevented in all 120 retracted sessions). We reproduce a silent lost update in ByteDance's deer-flow, formalizing its fix as a verified $L_0 \to L_1$ refinement, and exhibit tool-effect reordering in LangGraph's ToolNode on unmodified output, removed by an L3 commit-order sequencer. The verified detector, refinements, and realizability artifacts are the contribution; the phenomena and lattice are classical.

---

## 论文详细总结（自动生成）

# 多智能体大语言模型系统中并发异常的验证检测与预防：详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：多智能体 LLM 系统通过共享内存、向量索引和工具注册表协调工作，但当前缺乏对并发访问下一致性的形式化建模和可验证保障。经典一致性理论（硬件一致性模型、数据库隔离级别、分布式系统一致性）无法直接适用于 LLM 特有的长推理延迟、不可逆外部效应等特性。现有运行时刻（如 SagaLLM、Atomix 等）虽提供部分一致性保证，但未系统化命名异常、未提供分层设计空间或形式化证明。
- **整体含义**：本文首次为多智能体 LLM 运行时建立了机器检查的一致性层级（machine-checked consistency hierarchy），通过形式化定义四种并发异常（过时生成 A1、幻影工具 A2、因果级联 A3、工具效应重排序 A6），构造布尔格 \(L = \langle 2^{\{A1,A2,A3,A6\}}, \subseteq \rangle\)，并机械验证了其中一条最大链 \(L_0 \subsetneq L_1 \subsetneq L_2 \subsetneq L_3 \subsetneq L_4\) 的可实现性和严格分离。该贡献不仅是理论格，更是带有可验证检测器、精化证明和实际部署运行时的完整工件。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：借鉴经典数据库隔离级别分析框架（如 ANSI/SQL 隔离、Adya 模型），但针对多智能体 LLM 运行时的特定特征（长生成期、工具注册表作为一等可变状态、不可逆外部效应）进行改编。将操作建模为“读-生成-写”三阶段，使用 TLA⁺ 形式化四种异常，并用 Verus 对检测器（Rust 实现）与规格的等价性进行机械验证，同时对运行时策略的状态机精化进行验证。
- **关键技术细节**：
    - **操作模型**：每个操作记录包含读集、写集、读取时间、写入时间、工具注册表快照等。日志为 append-only。
    - **异常定义**：
        - **A1 (过时生成)**：操作 i 读取了某单元格的旧值，之后另一个操作 j 在 i 提交前写入了新值，且值不同。
        - **A2 (幻影工具)**：操作计划使用一个在开始读取时存在但提交时已移除的工具。
        - **A3 (因果级联)**：一个已提交的操作依赖了一个后续被中止的操作的输出（未补偿）。
        - **A6 (工具效应重排序)**：单个操作内多个外部效应的提交顺序与用户计划顺序不同。
    - **一致性层次**：在布尔格中选择一条最大链，每个 Ln 排除前 n 个异常：L0 允许所有，L1 排除 A1，L2 排除 A1+A3，L3 排除 A1+A3+A6，L4 排除所有四个。
    - **验证方法**：
        - 使用 TLA⁺ 和 TLC 模型检查器为每个异常生成有限参数下的反例（|A|=2, |Cells|≤2, MaxOps≤4）。
        - 使用 TLAPS 证明链定义之间的包含关系。
        - 使用 **Verus** 证明 Rust 检测器与规范的双向等价性（24 项义务，零 assume）。
        - 为三个已部署运行时（悲观锁、可串行化快照隔离 SSI、默认 SI）证明满足 ¬A1（40 项义务，零 assume）。
        - 通过四个精化证明（84 项义务）将运行时具体状态机映射到抽象状态机。
        - L2-L4 通过执行模式（exec-mode）验证，并配有无依赖的预防孪生体（dependency-free prevention twins）测量预防效果。
    - **预防机制**：
        - L0-L1：悲观锁（冲突操作丢弃）、SSI（提交时验证读集）、默认 SI（仅在写操作时验证）。
        - L2：因果跟踪运行时，提交时检查前驱均未中止；中止时级联到所有依赖。
        - L3：Saga 补偿机制，严格顺序提交工具调用（顺序定序器）。
        - L4：快照隔离工具注册表（pinned signature 验证）。

## 3. 实验设计：使用了哪些数据集/场景，benchmark 是什么，对比了哪些方法

- **数据集与场景**：
    1. **合成场景**：7 种可重现任务，包括并发过时读、工具移除、幻象写级联、工具效应重排序、线性管线、扇出合并、单智能体控制。共 700 个合成轨迹。
    2. **真实 LLM 场景**：使用 AutoGen 框架和 gpt-4o 在三个工作负载上运行：
        - **编辑-审查**（两个操作者读写同一文档，导致 100% A1 触发率）
        - **规划-执行**（严格顺序，A1 触发率约 1%）
        - **分类**（三智能体管线，A1 触发率约 35%）
    3. **Cookbook 场景**：基于 LangGraph/AutoGen 公开示例的六个场景（五个无状态工具、一个带类型共享状态），每个运行 100 次（gpt-4o）。
    4. **MAST-Data**：来自 Cemri 等人公开语料库的 1,242 个轨迹（7 个框架），成功解析 600 个并检测 A1。
    5. **LangGraph ToolNode 重排序测试**：在 LangGraph 1.2.5 中展示 A6，并用 L3 顺序定序器修复。
    6. **deer-flow 静默丢失更新复现**：在 ByteDance 项目上再现并形式化修复为 L0→L1 精化。
- **基准方法**：对比三种运行时策略：无保护（vanilla）、悲观锁（冲突操作直接丢弃）、快照隔离 SI（冲突提交中止）。在 900 个真实 LLM 会话上比较 A1 触发率和成本。
- **成本分析**：在 gpt-4o 和 Claude Sonnet 4.5 两个提供者上比较 tokens 开销和墙钟时间。
- **L2 A3 预防测量**：在合成级联场景（深度 2/3/5）和真实 LLM 会话（gpt-4o-mini, claude-haiku-4-5, Llama-3.2）上比较 L2 vs 无保护基线。

## 4. 资源与算力

- **未明确说明具体 GPU 型号、数量或训练时长**：文中使用的 LLM 为 API 调用（gpt-4o, Claude Sonnet 4.5, gpt-4o-mini, claude-haiku-4-5, Llama-3.2），未报告本地训练或微调资源。实验运行时间以会话数（约 1,800 个真实 LLM 会话）和模型检查状态数（TLC 最大约 9 百万状态，更大规模达 1.5 亿但未穷举）为主。无大规模 GPU 集群或长时间训练细节。作者明确提及“cost analysis”基于 API 计费和墙钟时间，无自建算力报告。

## 5. 实验数量与充分性

- **实验数量**：
    - 合成轨迹 700 个。
    - 真实 LLM 基线 900 会话（3 个工作负载 × 3 种运行时 × 100 会话 = 900）。
    - Cookbook 场景 600 会话（6 个场景 × 100）。
    - MAST-Data 解析 600 轨迹。
    - L2 A3 预防：合成级联 3,000 场景（1,000/深度 × 3 深度） + 真实 LLM 200 会话 × 3 模型系列（共 600），其中 120 有撤回。
    - 成本分析：额外高竞争扫描 155 配对会话（gpt-4o-mini）。
    - 墙钟时间研究：270 会话（3 策略 × 3 工作负载 × 30）在 gpt-4o 和 Llama-3.2 上各一次。
    - LangGraph 重排序：90 次运行（30/模型 × 3 模型）。
- **充分性评估**：
    - 实验覆盖了多种工作负载（从严格顺序到高并发）、运行时刻（vanilla、悲观、SI、SSI、L2因果）、模型家族（gpt-4o, Claude, Llama）和开源框架（AutoGen, LangGraph, deer-flow, Letta）。检测器在合成和真实 LLM 轨迹上均经过验证。
    - **不足之处**：A1 的观测率在工作负载工程化场景中为 1%-100%，在 cookbook 场景中仅在共享状态场景达到 90%，在 MAST-Data 中为 0/600。作者明确承认这些率非生产系统流行率，而是验证检测器可操作性。成本分析中跨提供者比较可能存在混淆（定价、令牌化差异被承认）。L2-L4 的实时运行仅 L2 在真实 LLM 上进行了；L3/L4 仅通过合成孪生体验证。未对所有 16 个格点进行实现分离，仅一条链。整体实验设计较充分，但作者透明地限定了范围。

## 6. 论文的主要结论与发现

- **主要结论**：
    1. **四种并发异常的形式化定义和机械验证**：检测器对于 TLA⁺ 规范是可靠且完备的（零 assume）。
    2. **从 L0 到 L4 的一致性层次可通过运行时策略实现和验证**：悲观锁和 SSI 能消除 A1（L1）；因果跟踪运行时消除 A1+A3（L2）；Saga 顺序定序器消除 A6（L3）；工具注册表快照消除 A2（L4）。
    3. **快照不足性**（Snapshot insufficiency）：结构定义的快照（读取时刻快照）不足以防止过时生成，需要显式读集稳定性（类似写偏移）。这是经典 write-skew 在新环境下的再现。
    4. **成本可接受**：SSI 在大多数工作负载下与无保护运行时的 tokens 开销无显著差异（配对分析显示约 8% 开销在一工作负载上不可忽略）；悲观锁开销有界（≤1.6× gpt-4o, ≤2.3× Claude），未达到数量级增长。
    5. **实际系统存在 A0 (丢失更新) 和 A6**：在 ByteDance deer-flow 中作为静默丢失更新出现（L0 级），在 LangGraph ToolNode 中作为工具效应重排序出现（A6）。这些均可被对应层次的预防机制消除。
    6. **MAST-Data 中 0/600 A1，表明 A1 在纯对话型多智能体框架中架构性地被避免**（因缺乏共享可变状态读-写冲突窗口）。
- **重要发现**：L2 因果跟踪在 120 个真实 LLM 撤回会话中预防了所有 A3（0/120）；L3 顺序定序器在多模型下消除了 A6 重排序。

## 7. 优点

- **正式方法与现实部署结合**：不仅给出理论格，还提供可运行的 Rust 运行时、经过 Verus 机械验证的检测器、精化证明，并在实际系统（deer-flow, LangGraph）中复现和修复异常。
- **验证深度高**：274 个 Verus 义务（零 assume/zero admit，仅两个结构公理和一个互斥体对应关系），覆盖检测器等价性、运行时安全性、精化、并发提升等。公共代码和信任基础完全透明。
- **实验设计多层次**：从合成测试到真实 LLM 模型，从标准化工作负载到实际框架集成，覆盖多个模型系列（闭源/开源），成本分析包括 tokens 和墙钟时间，并进行了配对检验和等价性测试。
- **明确局限性和未来工作**：作者诚实地列出了操作模型假设、确定性假设、无复制、有限界 TLC 验证、非概率判定等不足，并指出了修复路径（如概率细化、分离逻辑、更广生产部署检测等）。
- **机械验证的工件链完整**：从模型级（TLA⁺）到检测器（Rust + Verus）再到运行时精化，形成可追溯的验证链。L2-L4 的执行模式运行时同样经过验证（零额外公理），扩展了结果的可信度。
- **对现有系统的反向案例**：在 MAST-Data 中 A1 为零证明了大多数当前框架的默认行为已无意间避免了该异常（因缺乏跨会话可变共享状态），这与格理论预测一致。

## 8. 不足与局限

- **操作模型简化**：
    - 假设确定性生成，但实际 LLM 具有随机性；本文未对概率语义进行形式化。
    - 操作内的原子提交仅针对内部存储，外部效应可逆重排序被 A6 覆盖但假设外部效应不可逆。
    - 未考虑复制（因此 A4 被延迟到未来版本）；格中仅一条链被实现和验证，其余 15 个点未涉及。
- **验证范围受限**：
    - TLC 模型检查仅在有限参数下（|A|≤2, |Cells|≤2, MaxOps≤4）穷举；更大参数仅部分探索（150M 状态但未穷举）。
    - 精化证明仅覆盖单操作原子步骤，多线程并发通过 Mutex 信任基础提升，未用分离逻辑或线性化证明。
    - L3/L4 运行时仅执行模式验证和孪生体测量，未在真实 LLM 实时会话中运行。
- **实验限制**：
    - 真实 LLM 实验仅用两个模型家族（gpt-4o 和 Claude Sonnet 4.5），未覆盖所有主流模型。
    - A1 流行率在 cookbook 场景 90% 但在 MAST-Data 0/600，说明不同架构差异大，无法直接推广。
    - 成本分析中跨模型开销差异可能受计费结构和令牌化影响，作者声明仅做内模型比较。
    - L1/L2 实验使用了 AutoGen 框架，未覆盖其他框架（CrewAI、LangGraph 运行时等）。
- **未处理的问题**：
    - 未处理拜占庭或恶意智能体。
    - 未考虑流式写入（提交前部分外部化）。
    - 代价不对称性（LLM 推理成本高）仅通过实验报告，未纳入理论模型。

（完）
